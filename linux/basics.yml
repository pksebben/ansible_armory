---
- name: install basic tooling
  apt:
    pkg:
      - curl
      - python3
      - python3-pip
      - zsh
      - openssh-server
      - vim

- name: install pyenv
  command:
    cmd: "curl https://pyenv.run | bash"
